

python gentbf -i <vhdl_file>.vhd -f ods

tb_<vhdl_file>.vhd
tb_<vhdl_file>.ods

Edit tb_<vhdl_file>.ods (or xls) and repeate the last command. Another file was created
tb_<vhdl_file>.tbl

At this time only ods al xls are supported, 
